# Provider Capture Harness credentials are read only from the process environment.
# Do not source repository secrets, use dotenv, or commit populated values.
# Leave both values empty in this example and export only the selected provider key.
JINA_API_KEY=
MINERU_API_KEY=
# Optional Owner-controlled private HTTP proxy; generic proxy variables remain ignored.
PROVIDER_CAPTURE_PROXY_URL=
