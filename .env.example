# The canonical deployment template lives at:
#   mm-chat/.env.single-server.example
#
# Create a local runtime environment with:
#   cp mm-chat/.env.single-server.example mm-chat/.env.single-server
#   chmod 600 mm-chat/.env.single-server
#
# Keep real credentials only in the gitignored mm-chat/.env.single-server file.
